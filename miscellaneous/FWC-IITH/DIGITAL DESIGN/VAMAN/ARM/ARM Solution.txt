A multiplexer (or mux; spelled sometimes as multiplexor), also known as a data selector, is a device
that selects between several Analog or Digital input signals and forwards the selected input to a single
output line
F (X, Y, Z, W) = XYZW' + XY'ZW + X'Y(0) + X'Y'(Z' + W)
F (X, Y, Z, W) = XYZW' + XY'ZW + X'Y'Z' + X'Y'W
Now we can make it in cannonical form;
F (X, Y, Z, W) = XYZW' + XY'ZW + X'Y'Z' ( W + W') + X'Y'W(Z + Z')
F (X, Y, Z, W) = XYZW' + XY'ZW + X'Y'Z' W + X'Y'Z' W' + X'Y'WZ.
So finally we get,
F (X, Y, Z, W) = Σ m( 0, 1, 3,11,14)